kind: pipeline
name: klass-subsets-web

steps:
  - name: install
    image: node:current-alpine
    commands:
      - yarn install
    when:
      event:
        - pull_request
        - push

  - name: test
    image: node:current-alpine
    commands:
      - yarn coverage
    when:
      event:
        - pull_request
        - push
    depends_on:
      - install
